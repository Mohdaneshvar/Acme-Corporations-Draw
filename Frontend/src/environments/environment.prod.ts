export const environment = {
  production: true,

  title: document.title,
  name: require('package.json').name,
  version: require('package.json').version,
  publishDate: '2020-10-27 10:19',

  apiUrl: '/api/',
  captchaUrl: '/api/',
  apiVersion: 202007071046
};
